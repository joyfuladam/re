export interface Peer {
  id: string
  firstName: string
  lastName: string
  email: string | null
}

export interface SongOption {
  id: string
  title: string
}

export interface WorkOption {
  id: string
  title: string
}
